#include "main.h"
/**
 * more_numbers - Prints 1 to 14 ten times
 * using a nested for loop to implement
 * Return: Nothing
 */
void more_numbers(void)
{
	for (i = 0; i <= 9; i++)
		for (j = '0'; j <= '14'; j++)
		       if (j <=  9)
			       _putchar(j);
			else 
				

