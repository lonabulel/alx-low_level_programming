#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	char dh;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	for (dh = 'A'; dh <= 'Z'; ch++)
		putchar(dh);
	putchar('\n');

	return (0);
}



